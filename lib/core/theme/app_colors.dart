import 'package:flutter/material.dart';

/// Paleta de colores principal de la aplicación TutoríasApp.
class AppColors {
  AppColors._();

  // Colores primarios
  static const Color primary = Color(0xFF4A90D9);
  static const Color primaryDark = Color(0xFF2C6BB5);
  static const Color primaryLight = Color(0xFFAAD4F5);

  // Colores secundarios
  static const Color secondary = Color(0xFF34C759);
  static const Color secondaryDark = Color(0xFF248A3D);

  // Colores de fondo
  static const Color background = Color(0xFFF5F7FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color scaffoldBackground = Color(0xFFF0F2F5);

  // Colores de texto
  static const Color textPrimary = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textHint = Color(0xFF9CA3AF);

  // Colores de estado
  static const Color success = Color(0xFF34C759);
  static const Color warning = Color(0xFFFF9500);
  static const Color error = Color(0xFFFF3B30);
  static const Color info = Color(0xFF007AFF);

  // Colores de estrellas / rating
  static const Color starFilled = Color(0xFFFFB800);
  static const Color starEmpty = Color(0xFFD1D5DB);

  // Colores de tarjetas y bordes
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFF3F4F6);

  // Colores de badges de estado
  static const Color pendiente = Color(0xFFFFA726);
  static const Color confirmada = Color(0xFF66BB6A);
  static const Color completada = Color(0xFF42A5F5);
  static const Color cancelada = Color(0xFFEF5350);

  // Online / Presencial
  static const Color online = Color(0xFF7C4DFF);
  static const Color presencial = Color(0xFF00BCD4);
}
